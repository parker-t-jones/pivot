node_modules
dist
.expo
pnpm-lock.yaml
coverage
android
ios
# Canonical spec — avoid drive-by formatting churn
PLAN.md
# Plain-text copy-paste docs. Prettier rewrites their `===`/`---` underlines into
# `#` headings, which breaks the select-and-paste convention they exist for.
RUNBOOK.md
TRACK-B.md
# Generated by `pnpm db:types` (supabase CLI) — not hand-authored
**/database.types.ts
