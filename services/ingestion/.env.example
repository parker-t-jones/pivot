# Optional. Absent in local dev — Sentry reporting no-ops without it (see src/env.ts). Get a real
# value from the project's Sentry dashboard.
SENTRY_DSN=

# Tagged on every Sentry event's environment field. Defaults to "development".
SENTRY_ENVIRONMENT=development
